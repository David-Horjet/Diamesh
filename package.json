{
  "name": "diamesh",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "db:migrate": "npm run db:migrate --workspace=apps/api",
    "rag:ingest": "npm run rag:ingest --workspace=apps/api",
    "models:download": "npm run models:download --workspace=apps/api"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "bare-crypto": "^1.15.0",
    "@qvac/embed-llamacpp": "^0.18.2",
    "@qvac/llm-llamacpp": "^0.23.2"
  },
  "engines": {
    "node": ">=22.17.0",
    "npm": ">=10.9.0"
  }
}
